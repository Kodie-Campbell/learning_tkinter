# import tkinter module for gui
from tkinter import *

# define the root window
root = Tk()

# define a label with a message
myLabel = Label(root, text="Hello world")

# add the label to the root window
myLabel.pack()

# main loop to keep the window open
root.mainloop()
